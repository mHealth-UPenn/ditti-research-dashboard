/**
 * The props for the ActiveIcon component.
 * @property active - Whether the icon is active.
 * @property className - The class name of the icon.
 */
export interface ActiveIconProps {
  active: boolean;
  className?: string;
}
